commander = require("./command.js");

function command(message, args) {
    message.author.send("Please check your DM's");
}

module.exports = {
    execute: function (message, args) {
        
    }
}